version: "3.8"

services:
  attesting:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - attesting-data:/root/.attesting
    environment:
      - NODE_ENV=production
      - ATTESTING_PORT=3000
    restart: unless-stopped

volumes:
  attesting-data:
